# Copiá este archivo como .env si necesitás variables en el front.
# Vite solo expone las que empiezan con VITE_. .env NO se sube a git.

# Ejemplo: URL base de la API si algún día no usás el proxy de Vite.
# VITE_API_URL=http://localhost:3000
